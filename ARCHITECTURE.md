# 🏗️ Архитектура системы

## Обзор

Система построена по модульному принципу с разделением на слои:
1. **Слой данных** - модели и загрузка
2. **Слой бизнес-логики** - алгоритмы составления расписания
3. **Слой представления** - веб-интерфейс и экспорт

## Диаграмма компонентов

```
┌─────────────────────────────────────────────────────────────┐
│                    Веб-интерфейс (Streamlit)                │
│                       streamlit_app.py                       │
└────────────────────────────┬────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────┐
│              Генератор расписания + Экспорт                  │
│         schedule_generator.py  │  export_excel.py            │
└────────────────────────────┬────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────┐
│                    Загрузчик данных                          │
│                     data_loader.py                           │
└────────────────────────────┬────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────┐
│                  Базовые классы данных                       │
│                    schedule_base.py                          │
└─────────────────────────────────────────────────────────────┘
```

## Слой данных

### schedule_base.py

**Цель:** Определение всех базовых сущностей предметной области

**Основные классы:**

```python
# Перечисления
DayOfWeek       # Дни недели (понедельник-пятница)
SubjectType     # Типы предметов (обязательный, практикум ЕГЭ, факультатив)

# Сущности
TimeSlot        # Временной слот (день + номер урока)
Classroom       # Кабинет (номер, вместимость, этаж)
Teacher         # Учитель (ФИО, предметы, кабинет, недоступные дни)
Student         # Ученик (ФИО, класс, выбор ЕГЭ)
Class           # Класс (название, профиль, список учеников)
Subject         # Предмет (название, тип, учитель, часы)
EGEPracticeGroup # Группа для практикума ЕГЭ
Lesson          # Урок в расписании
Schedule        # Полное расписание с методами анализа
```

**Ключевые методы Schedule:**

- `is_teacher_busy()` - проверка занятости учителя
- `is_class_busy()` - проверка занятости класса
- `is_classroom_busy()` - проверка занятости кабинета
- `get_teacher_gaps()` - подсчет окон у учителя
- `get_class_gaps()` - подсчет окон у класса
- `save_to_json()` - сохранение в JSON

### data_loader.py

**Цель:** Загрузка всех данных из Excel файлов в объекты Python

**Класс `DataLoader`:**

**Методы загрузки:**

1. `load_classrooms(filename)` - загрузка кабинетов
   - Парсит Excel с информацией о кабинетах
   - Создает объекты `Classroom`
   - Связывает кабинеты с ответственными учителями

2. `load_teachers_and_subjects(filename)` - загрузка учителей и предметов
   - Парсит расстановку кадров на февраль
   - Создает объекты `Teacher` с их предметами
   - Создает объекты `Subject` для каждой связки учитель-предмет-класс
   - Учитывает учителей, не работающих по понедельникам

3. `load_students_and_ege_choices(filename)` - загрузка учеников
   - Парсит список участников ГИА-11
   - Создает объекты `Student` с выбором ЕГЭ
   - Группирует учеников по классам

4. `create_ege_practice_groups()` - формирование групп ЕГЭ
   - Группирует учеников по выбранным предметам ЕГЭ
   - Назначает учителей для каждой группы
   - Создает объекты `EGEPracticeGroup`

**Структура данных после загрузки:**

```python
loader.teachers: Dict[str, Teacher]        # Все учителя
loader.classrooms: Dict[str, Classroom]    # Все кабинеты
loader.classes: Dict[str, Class]           # Все классы
loader.students: Dict[str, Student]        # Все ученики
loader.subjects: List[Subject]             # Все предметы (связки)
loader.ege_groups: List[EGEPracticeGroup]  # Группы для ЕГЭ
```

## Слой бизнес-логики

### schedule_generator.py

**Цель:** Генерация оптимального расписания

**Класс `ScheduleGenerator`:**

**Алгоритм работы:**

```
Вход: DataLoader с загруженными данными
Выход: Schedule с размещенными уроками

Фаза 1: Размещение практикумов ЕГЭ ✅
  1. Оценить все возможные временные слоты
  2. Выбрать N лучших слотов (равномерно по неделе)
  3. В каждый слот разместить все практикумы параллельно
  4. Назначить кабинеты с учетом вместимости
  
Фаза 2: Размещение обязательных предметов ⏳
  1. Получить список всех обязательных предметов
  2. Отсортировать по приоритету (предметы с большим числом часов первыми)
  3. Для каждого предмета:
     - Найти свободные слоты
     - Проверить доступность учителя, класса, кабинета
     - Учесть приоритет времени (2-4 урок для сложных)
     - Разместить урок
  
Фаза 3: Оптимизация расписания ⏳
  1. Подсчитать метрики качества (окна, нагрузка по дням)
  2. Найти пары уроков для обмена
  3. Если обмен улучшает метрики - применить
  4. Повторять до тех пор, пока есть улучшения
```

**Ключевые методы:**

- `find_ege_practice_slots(num_slots)` - поиск оптимальных слотов для ЕГЭ
- `evaluate_ege_slot(slot)` - оценка качества слота для практикума ЕГЭ
- `place_ege_practices()` - размещение всех практикумов ЕГЭ
- `place_mandatory_subjects()` - размещение обязательных предметов (TODO)
- `optimize_schedule()` - оптимизация расписания (TODO)
- `find_available_classroom()` - поиск свободного подходящего кабинета

**Функция оценки слота:**

```python
def evaluate_ege_slot(slot: TimeSlot) -> float:
    score = 100.0
    
    # 1. Оценка по номеру урока
    if slot.lesson_number == 1:
        score -= 30  # Первый урок нежелателен
    elif slot.lesson_number == 7:
        score -= 20  # Последний урок нежелателен
    elif 2 <= slot.lesson_number <= 4:
        score += 20  # Оптимальное время
    
    # 2. Доступность учителей
    availability_ratio = доступные_учителя / всего_учителей
    score += availability_ratio * 50
    
    # 3. Случайный фактор
    score += random.uniform(-5, 5)
    
    return score
```

### export_excel.py (TODO)

**Цель:** Экспорт расписания в читаемый Excel файл

**Планируемая структура:**

```python
class ExcelExporter:
    def __init__(self, schedule: Schedule):
        self.schedule = schedule
    
    def export_by_classes(self, filename: str):
        """Экспорт расписания по классам"""
        # Создать Excel с листом для каждого класса
        # Формат: строки - уроки, столбцы - дни недели
        
    def export_by_teachers(self, filename: str):
        """Экспорт расписания по учителям"""
        # Создать Excel с листом для каждого учителя
        
    def export_summary(self, filename: str):
        """Экспорт сводной информации"""
        # Статистика, окна, нагрузка
```

## Слой представления

### streamlit_app.py (TODO)

**Цель:** Веб-интерфейс для работы с системой

**Планируемая структура:**

```python
import streamlit as st

# Главная страница
def main():
    st.title("Генератор расписания")
    
    # Боковая панель с настройками
    with st.sidebar:
        st.header("Настройки")
        uploaded_files = st.file_uploader("Загрузить файлы", 
                                         accept_multiple_files=True)
    
    # Основная область
    tab1, tab2, tab3 = st.tabs(["Генерация", "Расписание", "Статистика"])
    
    with tab1:
        # Кнопка генерации
        if st.button("Сгенерировать расписание"):
            run_generation()
    
    with tab2:
        # Визуализация расписания
        show_schedule()
    
    with tab3:
        # Статистика и метрики
        show_statistics()
```

**Функции:**

1. `run_generation()` - запуск генератора расписания
2. `show_schedule()` - отображение расписания (по классам/учителям)
3. `show_statistics()` - показ статистики (окна, нагрузка)
4. `highlight_problems()` - подсветка проблемных мест
5. `export_results()` - скачивание результата

## Принципы проектирования

### 1. Модульность
Каждый модуль отвечает за свою область ответственности

### 2. Разделение данных и логики
Классы данных (`schedule_base.py`) не содержат бизнес-логики

### 3. Расширяемость
Легко добавить новые ограничения или критерии оптимизации

### 4. Тестируемость
Каждый модуль можно тестировать независимо

### 5. Читаемость
Код документирован, используются понятные имена

## Потоки данных

### Генерация расписания

```
Excel файлы
    ↓
DataLoader.load_*()
    ↓
Объекты Python (Teacher, Student, Class, etc.)
    ↓
ScheduleGenerator.place_ege_practices()
    ↓
Schedule с практикумами ЕГЭ
    ↓
ScheduleGenerator.place_mandatory_subjects()
    ↓
Schedule с полным расписанием
    ↓
ScheduleGenerator.optimize_schedule()
    ↓
Оптимизированное расписание
    ↓
ExcelExporter.export_*()
    ↓
Excel файл с результатом
```

### Веб-интерфейс

```
Пользователь загружает Excel
    ↓
Streamlit вызывает DataLoader
    ↓
DataLoader создает объекты
    ↓
Streamlit отображает загруженные данные
    ↓
Пользователь нажимает "Сгенерировать"
    ↓
Streamlit вызывает ScheduleGenerator
    ↓
ScheduleGenerator возвращает Schedule
    ↓
Streamlit отображает расписание
    ↓
Пользователь скачивает Excel
```

## Обработка ошибок

### Валидация на входе

```python
# В DataLoader
if not teachers:
    raise ValueError("Нет данных об учителях")

if len(classes) == 0:
    raise ValueError("Нет данных о классах")
```

### Проверка конфликтов

```python
# В ScheduleGenerator
if schedule.is_teacher_busy(teacher, slot):
    # Логировать конфликт
    logger.warning(f"Учитель {teacher.name} уже занят в {slot}")
    # Попытаться найти другой слот
    slot = find_alternative_slot(teacher, subject)
```

### Откат изменений

```python
# При оптимизации
original_schedule = copy.deepcopy(schedule)
try:
    optimize_schedule(schedule)
    if quality_score(schedule) < quality_score(original_schedule):
        # Откатываемся
        schedule = original_schedule
except Exception as e:
    logger.error(f"Ошибка оптимизации: {e}")
    schedule = original_schedule
```

## Метрики качества

### 1. Количество окон

```python
total_gaps = sum(schedule.get_teacher_gaps(teacher) 
                 for teacher in teachers.values())
```

### 2. Стандартное отклонение нагрузки

```python
import numpy as np

# Количество уроков по дням для каждого учителя
daily_loads = []
for teacher in teachers.values():
    for day in DayOfWeek:
        count = len(schedule.get_lessons_by_teacher_and_day(teacher, day))
        daily_loads.append(count)

std_dev = np.std(daily_loads)
```

### 3. Процент уроков в оптимальное время

```python
optimal_lessons = sum(
    1 for lesson in schedule.lessons
    if 2 <= lesson.time_slot.lesson_number <= 4
)

optimal_ratio = optimal_lessons / len(schedule.lessons)
```

## Производительность

### Оценка сложности

- **Фаза 1 (ЕГЭ):** O(n * m), где n - число слотов, m - число групп
- **Фаза 2 (предметы):** O(p * s), где p - число предметов, s - число слотов
- **Фаза 3 (оптимизация):** O(k * l^2), где k - итерации, l - число уроков

### Ожидаемое время работы

- Загрузка данных: < 1 сек
- Фаза 1: < 5 сек
- Фаза 2: 10-30 сек
- Фаза 3: 30-60 сек

**Общее время:** 1-2 минуты

## Будущие улучшения

### Версия 0.2

- [ ] Реализовать Фазу 2 и 3
- [ ] Добавить экспорт в Excel
- [ ] Базовый веб-интерфейс

### Версия 0.3

- [ ] Продвинутая оптимизация (генетические алгоритмы)
- [ ] Ручная корректировка расписания
- [ ] История изменений

### Версия 1.0

- [ ] Поддержка нескольких корпусов
- [ ] Интеграция с внешними системами
- [ ] API для других приложений

---

**Документ обновлен:** 31.01.2026  
**Версия архитектуры:** 1.0
