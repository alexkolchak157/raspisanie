# 🚀 Быстрый старт для Claude Code

## Шаг 1: Подготовка проекта

### 1.1 Создайте структуру папок

```
schedule-automation/
├── schedule_base.py           ✅ Готов
├── data_loader.py             ✅ Готов
├── schedule_generator.py      ✅ Готов (Фаза 1)
├── phase2_mandatory.py        ⏳ Шаблон (нужно доработать)
├── phase3_optimization.py     ⏳ Шаблон (нужно доработать)
├── main.py                    ✅ Готов
├── README.md                  ✅ Готова документация
├── requirements.txt           ✅ Готов
└── data/                      📁 Создайте эту папку
    ├── Здания__кабинеты__места__школьные_здания_.xlsx
    ├── Расстановка_кадров_ФЕВРАЛЬ_2025-2026_учебный_год__2_.xlsx
    └── Список_участников_ГИА-11_ГБОУ_Школа__Покровский_квартал___41_.xlsx
```

### 1.2 Скопируйте Excel файлы

Поместите все 3 Excel файла с данными в папку `data/`:
- Кабинеты
- Расстановка кадров (февраль)
- Список участников ГИА-11

## Шаг 2: Установка зависимостей

```bash
pip install -r requirements.txt
```

Или вручную:
```bash
pip install pandas openpyxl xlrd
```

## Шаг 3: Запуск

### Быстрый тест (только Фаза 1)

```bash
python main.py --phase 1
```

**Результат:** 
- Создастся файл `output/schedule_phase1.json`
- В терминале увидите статистику: 53 урока практикумов ЕГЭ размещены

### Полный запуск (все фазы)

```bash
python main.py --phase all
```

**Важно:** Фазы 2 и 3 пока не реализованы, нужно сначала доработать!

## Шаг 4: Следующие задачи

### ✅ Что работает сейчас

1. **Загрузка данных** - все Excel файлы успешно парсятся
2. **Фаза 1: Практикумы ЕГЭ** - автоматически находит оптимальные слоты и размещает 53 урока

### 🔴 Приоритет 1: Доработать Фазу 2

**Файл:** `phase2_mandatory.py`

**Что нужно сделать:**
1. Реализовать метод `place_all_mandatory_subjects()`
2. Доработать `_evaluate_slot()` - добавить более умную оценку
3. Реализовать `_find_classroom()` - умный поиск кабинета
4. Обработать деление на группы (иностранные языки)

**Подсказка для Claude Code:**
```
Открой файл phase2_mandatory.py и найди все места с TODO.
Начни с метода _place_subject() - это основной алгоритм размещения.
Используй уже реализованные методы из schedule_base.py:
- schedule.is_teacher_busy()
- schedule.is_class_busy()
- schedule.is_classroom_busy()
```

### 🟡 Приоритет 2: Доработать Фазу 3

**Файл:** `phase3_optimization.py`

**Что нужно сделать:**
1. Проверить алгоритм simulated annealing
2. Добавить больше метрик качества
3. Протестировать на реальных данных

### 🟢 Приоритет 3: Создать веб-интерфейс

**Файл:** `streamlit_app.py` (нужно создать)

**Что нужно сделать:**
1. Базовый интерфейс с загрузкой файлов
2. Визуализация расписания
3. Экспорт в Excel

## Шаг 5: Отладка

### Проверка загруженных данных

```python
python data_loader.py
```

Должно вывести:
- 29 кабинетов
- 42 учителя
- 256 учеников
- 15 групп для практикумов ЕГЭ

### Проверка Фазы 1

```python
python schedule_generator.py
```

Должно вывести:
- 4 оптимальных слота найдено
- 53 урока практикумов ЕГЭ размещены

### Проверка конфликтов

Добавьте в конец `main.py`:

```python
# Проверка конфликтов
def check_conflicts(schedule):
    conflicts = []
    
    for i, lesson1 in enumerate(schedule.lessons):
        for lesson2 in schedule.lessons[i+1:]:
            if lesson1.time_slot == lesson2.time_slot:
                # Один учитель в двух местах
                if lesson1.teacher == lesson2.teacher:
                    conflicts.append(f"Учитель {lesson1.teacher.name} в двух местах: {lesson1.time_slot}")
                
                # Один класс в двух местах
                if lesson1.class_or_group == lesson2.class_or_group:
                    conflicts.append(f"Класс {lesson1.class_or_group} в двух местах: {lesson1.time_slot}")
    
    return conflicts

conflicts = check_conflicts(generator.schedule)
if conflicts:
    print("\n⚠️  НАЙДЕНЫ КОНФЛИКТЫ:")
    for c in conflicts[:10]:
        print(f"   {c}")
else:
    print("\n✅ Конфликтов не найдено")
```

## Подсказки для Claude Code

### Как работать с проектом

1. **Откройте промпт:** `CLAUDE_CODE_PROMPT.md`
   - Там полное описание проекта
   - Все требования и ограничения
   - План дальнейшей разработки

2. **Изучите архитектуру:** `ARCHITECTURE.md`
   - Диаграммы компонентов
   - Описание всех классов
   - Алгоритмы работы

3. **Следуйте плану:** `DEVELOPMENT_PLAN.md`
   - Пошаговые задачи
   - Приоритеты
   - Примеры кода

### Полезные команды для отладки

```python
# Посмотреть все уроки учителя
teacher = loader.teachers["Иванов И.И."]
lessons = generator.schedule.get_lessons_by_teacher(teacher.name)
for lesson in lessons:
    print(lesson)

# Посмотреть расписание класса
lessons = generator.schedule.get_lessons_by_class("11В")
for lesson in lessons:
    print(lesson)

# Посмотреть окна у учителя
gaps = generator.schedule.get_teacher_gaps(teacher)
print(f"Окон: {gaps}")

# Сохранить в JSON
generator.schedule.save_to_json('output/my_schedule.json')
```

## Часто встречающиеся ошибки

### 1. FileNotFoundError: файлы данных не найдены

**Решение:** Убедитесь, что Excel файлы находятся в папке `data/`

### 2. ImportError: нет модуля pandas

**Решение:** Установите зависимости: `pip install -r requirements.txt`

### 3. KeyError при парсинге Excel

**Решение:** Проверьте структуру Excel файла, возможно изменились названия столбцов

### 4. Все практикумы назначены одному учителю

**Решение:** Это известная проблема, нужно доработать `create_ege_practice_groups()` в `data_loader.py`

## Контакты

**Разработчик:** Роман, учитель обществознания ГБОУ "Школа Покровский квартал"

**Статус проекта:** Альфа v0.1 (40% готовности)

**Следующая версия:** После реализации Фазы 2

---

**Удачи! 🚀**
